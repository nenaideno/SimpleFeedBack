let initialState: {

} = {
    
}

let generatorReducer = (state=initialState, action: {type: string, data: any}) => {
    switch (action.type) {
        default:
            return state
    }
}

// actions

export default generatorReducer;